# Eskuel game-package recipes

Game-package recipes are authoring inputs for the `eskuelgame pack` command. They are not files inside `.eskuelgame` archives and are versioned independently from the game-package format. The current recipe format is [version 1](./v1/README.md).
