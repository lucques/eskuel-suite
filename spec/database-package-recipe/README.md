# Eskuel database-package authoring recipes

An Eskuel database-package authoring recipe supplies metadata and local input paths to the `eskueldb pack` command. It is an authoring format rather than part of the generated `.eskueldb` archive.

The current format is [version 1](./v1/README.md).
